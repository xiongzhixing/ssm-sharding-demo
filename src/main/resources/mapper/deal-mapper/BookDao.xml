<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soecode.lyf.dao.deal.BookDao">
    <resultMap id="BaseResultMap" type="BookDO">
        <result column="book_id" jdbcType="BIGINT" property="bookId"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="number" jdbcType="INTEGER" property="number"/>
    </resultMap>
    <sql id="Base_Column_List">
        book_id, name, number
    </sql>
    <insert id="insert" parameterType="BookDO">
        insert into book (<include refid="Base_Column_List" />)
        values (#{bookId,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, #{number,jdbcType=INTEGER})
    </insert>
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryById" resultMap="BaseResultMap" parameterType="long">
        SELECT <include refid="Base_Column_List" />
        FROM book WHERE book_id = #{bookId}
    </select>
    <select id="queryAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM book WHERE 1=1
		ORDER BY book_id LIMIT #{offset}, #{limit}
	</select>
    <select id="queryByName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM book WHERE 1=1
        <if test="name!=null and name!=''">
            and name=${name}
        </if>
    </select>
    <select id="queryByBookIdList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM book WHERE 1=1 and book_id in
        <foreach collection="bookIdList" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
    <update id="reduceNumber">
		UPDATE book
		SET number = number - 1
		WHERE
			book_id = #{bookId}
		AND number > 0
	</update>
</mapper>