<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soecode.lyf.dao.idgenerate1.BookIdGenerateDao1">
  <insert id="replace" useGeneratedKeys="true" keyProperty="id" parameterType="com.soecode.lyf.entity.idgenarate.BookIdGenerate">
    replace into book_id_generate_${params.tabIndex} (name) values (${params.name})
  </insert>

  <select id="selectAll" resultType="BookIdGenerate" >
    SELECT id,NAME from book_id_generate_${params.tabIndex}
  </select>
</mapper>